---
description: "Output a mermaid dependency graph of all markdown links."
allowed-tools: Bash
---

Run the following command to generate the markdown link dependency graph:

```bash
python3 "${CLAUDE_PLUGIN_ROOT}/run.py" graph
```

Show the mermaid output to the user.
