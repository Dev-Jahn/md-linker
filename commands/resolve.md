---
description: "Remove all stale-refs frontmatter annotations from all files."
allowed-tools: Bash
---

Run the following command to resolve all stale references:

```bash
python3 "${CLAUDE_PLUGIN_ROOT}/run.py" resolve
```

Report the output to the user.
